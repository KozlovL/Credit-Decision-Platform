apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-selection-service-lkozlov
  namespace: test
  labels:
    student: lkozlov
    app: flow-selection-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-selection-service
      student: lkozlov
  template:
    metadata:
      labels:
        app: flow-selection-service
        student: lkozlov
    spec:
      containers:
        - name: flow-selection-service
          image: shift.gitlab.yandexcloud.net:5050/shift-python/y2025/homeworks/kozlov-l/shift_project/flow-selection-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: CONFIG_FILE
              value: "config.k8s.yaml"
          volumeMounts:
            - name: config-volume
              mountPath: /app/src/config.k8s.yaml
              subPath: config.k8s.yaml
      volumes:
        - name: config-volume
          configMap:
            name: app-config-lkozlov
      imagePullSecrets:
        - name: lkozlov-container-registry